*.pyc
*.o
*.d
*.so
*.pcm
*.log
*.png
*.pdf
condor_*.sub
results/
.__afsB28E
.__afs0E8E
.__afs0DB3

SubmitAll.sh

etc/*.pyc
etc/config/*.pyc

condor/*
.DS_Store

# fit_diagnostics/ 是每次擬合都重生的診斷輸出（6741 個檔），追蹤它會讓
# git status 永遠掛著 5000+ 個 modified，實質上讓 git 無法用來看真正的程式碼改動。
# 2026-09-06 取消追蹤，檔案本身保留在磁碟上。
fit_diagnostics/
# condor job log 已改為壓縮封存在 condor_fit/log_archive/，不要再追蹤散檔
condor_fit/logs/
condor_fit/eos_retry_logs/
condor_fit/scratch/
# ROOT ACLiC 的編譯暫存（檔名含 PID，每次編譯都不同）與共享物件
libCpp/*_ACLiC_dict*
libCpp/*.so
libCpp/*.d
libCpp/*.o
libCpp/*.pcm
# Cython 由 histUtils.pyx 生成，不需要版控（第一行即 "Generated by Cython"）
libPython/histUtils.cpp
libPython/*.so
libPython/*.o
# 一次性重跑的 log 目錄（產物在 EOS，log 只在本機有意義）
logs_*/
summary_logs_*/
window_logs_*/
rebin_logs_*/
libCpp_stale_*/
